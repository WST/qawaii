# Инструкции для AI-ассистентов

Этот документ содержит важную информацию для AI-ассистентов, работающих над проектом Qawaii, чтобы обеспечить преемственность разработки и понимание архитектуры проекта.

## О проекте

**Qawaii** — это приложение для управления заметками на Qt с поддержкой различных драйверов хранилища данных. Проект использует классический интерфейс с меню, тулбаром, сайдбаром и контент-панелью.

### Ключевые особенности
- Двухуровневая иерархия: папки → заметки
- Поддержка Markdown для форматирования заметок
- Модульная архитектура с абстрактными драйверами хранилища
- Кроссплатформенность (Linux, Windows, macOS)
- Локализация через Qt (русский и английский языки)

## Архитектура проекта

### Структура директорий

```
qawaii/
├── CMakeLists.txt              # Основной файл сборки
├── src/
│   ├── main.cpp                # Точка входа
│   ├── core/                   # Ядро приложения
│   │   ├── models/             # Модели данных (Folder, Note)
│   │   ├── storage/            # Драйверы хранилища
│   │   └── services/           # Бизнес-логика
│   ├── ui/                     # UI компоненты
│   │   ├── widgets/            # Виджеты (NotesTreeWidget, NoteEditorWidget)
│   │   └── dialogs/            # Диалоги (SettingsDialog)
│   └── utils/                  # Утилиты (MarkdownParser)
├── resources/                   # Ресурсы и локализация
│   ├── icons/                  # Иконки
│   └── translations/           # Файлы переводов (.ts)
├── tests/                      # Модульные тесты
└── plan.md                     # Подробный план разработки
```

### Слои архитектуры

1. **Слой данных (Data Layer)**
   - `StorageDriver` — абстрактный интерфейс драйвера
   - `SQLiteDriver` — реализация для SQLite (✅ полностью реализован)
   - `MySQLDriver` — реализация для MySQL (⚠️ заглушка)

2. **Слой бизнес-логики (Business Logic Layer)**
   - `NotesManager` — управление заметками и папками
   - `SyncService` — синхронизация данных
   - `MigrationService` — миграция между драйверами

3. **Слой представления (Presentation Layer)**
   - `MainWindow` — главное окно приложения
   - `NotesTreeWidget` — дерево заметок (сайдбар)
   - `NoteEditorWidget` — редактор заметок

## Технический стек

- **Язык**: C++17
- **Фреймворк**: Qt 6.x (совместимо с Qt 5.15+)
- **Система сборки**: CMake 3.16+
- **Тестирование**: Qt Test Framework
- **Базы данных**: SQLite (локально), MySQL (сетевое хранилище)
- **Кодировка**: UTF-8 везде

## Текущий статус проекта

### ✅ Выполнено

- Базовая структура проекта и система сборки
- Модели данных (Folder, Note)
- Абстрактный интерфейс StorageDriver
- Полная реализация SQLiteDriver с использованием QStandardPaths
- Базовые сервисы (NotesManager, SyncService, MigrationService)
- Базовый UI (MainWindow, виджеты)
- Система локализации Qt
- **18 модульных тестов для SQLiteDriver (все проходят)**

### ⚠️ В процессе / Частично

- MySQL драйвер (заглушка, требует реализации)
- Интеграция драйверов с UI
- Функциональность виджетов (отображение данных)
- Редактор Markdown
- Диалоги настроек

### 📋 Следующие шаги

1. Интеграция SQLite драйвера с NotesManager в UI
2. Реализация функциональности виджетов (отображение заметок и папок)
3. Реализация MySQL драйвера
4. Добавление тестов для MySQL драйвера
5. Реализация редактора Markdown

## Важные соглашения и правила

### Кодирование

1. **Кодировка**: Все файлы в UTF-8
2. **Комментарии**: На русском языке
3. **Стиль кода**: Следовать Qt Coding Style
4. **Именование**: 
   - Классы: `PascalCase` (например, `NotesManager`)
   - Методы: `camelCase` (например, `createFolder`)
   - Приватные члены: префикс `m_` (например, `m_driver`)

### Работа с драйверами

- Все драйверы наследуются от `StorageDriver`
- Драйверы должны быть потокобезопасными (если используются в многопоточности)
- Использовать транзакции для атомарных операций
- Обрабатывать все ошибки и логировать их через `qWarning()`

### Работа с путями

- Использовать `QStandardPaths` для кроссплатформенных путей
- SQLite по умолчанию: `QStandardPaths::writableLocation(QStandardPaths::AppConfigLocation)/notes.db`
- Создавать директории автоматически при необходимости

### Тестирование

- Все новые драйверы должны иметь полный набор тестов
- Тесты находятся в `tests/`
- Запуск тестов: `cd build && ctest` или `./TestSQLiteDriver`
- Тесты должны быть изолированными (использовать временные БД)

### Git workflow

- Коммиты делает пользователь самостоятельно
- Не нужно автоматически коммитить изменения
- Проект размещён на GitHub

## Работа с базой данных

### SQLite схема

```sql
CREATE TABLE folders (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    icon_name TEXT DEFAULT 'folder',
    created_at TEXT NOT NULL,
    modified_at TEXT NOT NULL
);

CREATE TABLE notes (
    id TEXT PRIMARY KEY,
    folder_id TEXT NOT NULL,
    title TEXT NOT NULL,
    content TEXT DEFAULT '',
    icon_name TEXT DEFAULT 'note',
    created_at TEXT NOT NULL,
    modified_at TEXT NOT NULL,
    version TEXT DEFAULT '1',
    FOREIGN KEY (folder_id) REFERENCES folders(id) ON DELETE CASCADE
);
```

**Важно**: В SQLite нужно включать поддержку внешних ключей:
```cpp
query.exec("PRAGMA foreign_keys = ON");
```

### Идентификаторы

- Использовать `QUuid::createUuid().toString(QUuid::WithoutBraces)` для генерации ID
- Все ID хранятся как TEXT в БД

### Версионирование заметок

- При каждом обновлении заметки версия увеличивается на 1
- Версия хранится как строка (для будущей поддержки семантического версионирования)

## Локализация

- Все пользовательские строки должны использовать `tr()`
- Файлы переводов: `resources/translations/qawaii_ru.ts` и `qawaii_en.ts`
- После добавления новых строк нужно обновить .ts файлы:
  ```bash
  lupdate src/ -ts resources/translations/qawaii_ru.ts
  ```

## Сборка и запуск

### Сборка проекта

```bash
mkdir build && cd build
cmake ..
make
```

### Запуск тестов

```bash
cd build
ctest                    # Через CTest
./TestSQLiteDriver       # Прямой запуск
./TestSQLiteDriver -v2   # Подробный вывод
```

### Запуск приложения

```bash
cd build
./Qawaii
```

## Работа с планом разработки

- Файл `plan.md` содержит подробный план разработки
- **Важно**: Регулярно обновлять статус выполнения этапов в `plan.md`
- Отмечать выполненные задачи как `[x]`
- Обновлять секцию "Текущий статус проекта" при значительных изменениях

## Частые задачи

### Добавление нового драйвера

1. Создать класс, наследующийся от `StorageDriver`
2. Реализовать все виртуальные методы
3. Добавить в CMakeLists.txt
4. Создать тесты в `tests/`
5. Обновить `plan.md`

### Добавление нового UI компонента

1. Создать класс виджета в `src/ui/widgets/`
2. Добавить в CMakeLists.txt
3. Интегрировать в MainWindow
4. Добавить локализацию строк
5. Обновить `plan.md`

### Исправление багов

1. Воспроизвести проблему
2. Написать тест, который демонстрирует баг (если возможно)
3. Исправить код
4. Убедиться, что все тесты проходят
5. Обновить документацию при необходимости

## Полезные ссылки

- **План разработки**: `plan.md` — содержит детальную архитектуру и пошаговый план
- **README**: `README.md` — базовая информация о проекте
- **Исходный запрос**: `prompt.txt` — первоначальное описание проекта

## Вопросы и проблемы

Если возникают вопросы по архитектуре или реализации:

1. Проверьте `plan.md` — там может быть ответ
2. Изучите существующий код SQLiteDriver как пример реализации
3. Посмотрите тесты — они показывают ожидаемое поведение
4. Следуйте принципам SOLID и паттернам, описанным в `plan.md`

## Важные напоминания

- ✅ Всегда обновляйте `plan.md` после выполнения задач
- ✅ Запускайте тесты перед завершением работы
- ✅ Используйте UTF-8 для всех строк
- ✅ Комментируйте код на русском языке
- ✅ Следуйте существующему стилю кода
- ✅ Не коммитить изменения автоматически (пользователь делает это сам)

---

**Последнее обновление**: 2024-12-02  
**Версия проекта**: 1.0.0 (в разработке)

